# Symbio AI Editable Source for Ravi / Claude Code

This is Mohammed's editable Eleventy source for the Symbio AI customer website.

## Quick start

```bash
npm install
npm run build
npm run lint
npx @11ty/eleventy --serve --port 8895
```

## What to edit

- Main source lives in `src/`.
- Built output is generated into `dist/`; do not edit `dist/` directly.
- Keep the site customer-facing: premium websites, redesigns, apps, dashboards, chatbots, and AI agents.
- Keep copy human and direct. Avoid fake testimonials, personal GitHub links, private files, and em dashes.
- Public email: contact@symbioai.dev.

## Important guardrails

- Do not include private workflow files, resumes, API keys, `.env` files, deploy secrets, or private notes.
- If Claude improves the design, send back the edited source or a zip with a changelog so Mohammed/Codex can merge and deploy.
